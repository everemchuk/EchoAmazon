---
import App from "@app/App.astro";

export function getStaticPaths() {
	return [{ params: { slug: "sample-product" } }];
}

const { slug } = Astro.params;

// Dynamic product details page based on slug
---

<App title={`Product - ${slug}`} />
